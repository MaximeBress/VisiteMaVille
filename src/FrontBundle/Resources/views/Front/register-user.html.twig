{% extends 'base.html.twig' %}
{% block title %}Inscription compte utilisateur{% endblock %}
{% block body %}
<div class="container">
    <div class="row">
        <div class="col-sm-offset-2 col-md-8 col-md-offset-3 col-md-6">
            <a href="{{ path('front_homepage') }}">
                <img src="{{ asset('bundles/front/image/logo_black.png')}}" class="img-responsive" />
            </a>
            {{ form_start(form) }}
                <div class="form-group">
                    <label>Votre nom</label>
                    {{ form_widget(form.nom) }}
                </div>
                <div class="form-group">
                    <label>Votre prénom</label>
                    {{ form_widget(form.prenom) }}
                </div>
                <div class="form-group">
                    <label>Votre adresse email</label>
                    {{ form_widget(form.email) }}
                </div>
                <div class="form-group">
                    <label>Votre mot de passe</label>
                    {{ form_widget(form.plainPassword) }}
                </div>
                {{ form_widget(form.creer) }}
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
